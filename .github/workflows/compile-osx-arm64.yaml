name: isoextract-osx-arm64

# Per-architecture compile + test for osx-arm64. Thin wrapper that delegates to the reusable
# base workflow (compile.yaml).

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/compile-osx-arm64.yaml'
      - '.github/workflows/compile.yaml'
      - 'src/**'
      - 'build.sh'
      - 'tests/data/**'

jobs:
  compile:
    uses: ./.github/workflows/compile.yaml
    with:
      rid: osx-arm64
      os: macos-latest
